[gd_scene load_steps=3 format=3]

# External resources used by this scene.  Assign the player sprite and
# script here.  The id numbers must be unique within this file.
[ext_resource type="Texture2D" path="res://assets/character.png" id=1]
[ext_resource type="Script" path="res://scripts/Player.gd" id=2]

# The root of the Player scene is a CharacterBody2D.  It will handle
# input and movement logic via the attached script.  The child nodes
# define the visual appearance and collision shape.
[node name="Player" type="CharacterBody2D"]
script = ExtResource(2)

[node name="Sprite2D" type="Sprite2D" parent="Player"]
texture = ExtResource(1)

[node name="CollisionShape2D" type="CollisionShape2D" parent="Player"]
# A simple rectangle collider roughly fitting the stickman sprite.  You
# can adjust the extents to better match the imported artwork.
shape = RectangleShape2D {
    extents = Vector2(20, 40)
}